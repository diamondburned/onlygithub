schema: /tmp/onlygithub/schema.docs.graphql
generated: queries.graphql.gen.go
operations: [queries.graphql]
# Base64String
# BigInt
# Boolean
# Date
# DateTime
# Float
# GitObjectID
# GitRefname
# GitSSHRemote
# GitTimestamp
# HTML
# ID
# Int
# PreciseDateTime
# String
# URI
# X509Certificate
bindings:
  Base64String: { type: string }
  BigInt: { type: int64 }
  Boolean: { type: bool }
  Date: { type: time.Time }
  DateTime: { type: time.Time }
  Float: { type: float64 }
  GitObjectID: { type: string }
  GitRefname: { type: string }
  GitSSHRemote: { type: string }
  GitTimestamp: { type: time.Time }
  HTML: { type: string }
  ID: { type: string }
  Int: { type: int32 }
  PreciseDateTime: { type: time.Time }
  String: { type: string }
  URI: { type: string }
  X509Certificate: { type: string }
